# CONSISTENCY 域 API 文档

本目录包含 `mosaic.nodes.consistency` 下所有节点的自动生成 API 文档。

## 节点列表

| 节点 | 说明 |
|------|------|
| `IdentityKeeper` | 角色一致性：参考图 + 驱动图，保持人物身份不变 |
| `StyleKeeper` | 风格一致性：参考风格图 + 目标图，保持视觉风格一致 |
| `CrossFrameConsistency` | 帧间一致性：逐帧分析，确保视频帧间连贯 |
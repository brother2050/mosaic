# SUBTITLE 域 API 文档

本目录包含 `mosaic.nodes.subtitle` 下所有节点的自动生成 API 文档。

## 节点列表

| 节点 | 说明 |
|------|------|
| `SubtitleGenerator` | 语音 → 字幕，支持多种语言自动识别 |
| `SubtitleTranslator` | 字幕翻译，支持多语言互译 |
| `SubtitleAligner` | 字幕对齐，将译文与原文时间轴对齐 |